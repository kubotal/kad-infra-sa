

componentReleases:

  - name: minio2
    component:
      name: minio
      version: 1.0.0
      protected: true
      config:
        install:
          createNamespace: true
      parameters:
        ingressName: minio2
        ldap: openldap
      parameterFiles:
        - document: minio-flavor-small
        - document: data2-minio-parameters
          unwrap: minio
    namespace: minio2
    dependsOn:
      - ldapLocalServer
