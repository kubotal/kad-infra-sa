templateReleases:

  - name: minio3
    template:
      name: minio-1
      version: 1.0.1
      parameters:
        namespace: minio3
        capacityGb: 10
        ingressName: minio3
        admin:
          rootUser: minio
          rootPassword: minio123
