
templateReleases:

  - name: minio1
    template:
      name: minio-1
      version: 1.0.0
      parameters:
        namespace: minio1
        capacityGb: 10
        ingressName: minio1
      parameterFiles:
        - document: data1-minio-parameters
          unwrap: minio
          wrap: admin
