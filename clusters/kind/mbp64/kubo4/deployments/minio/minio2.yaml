
templateReleases:

  - name: minio2
    template:
      name: minio-1
      version: 1.0.0
      parameters:
        namespace: minio2
        capacityGb: 10
        ingressName: minio2
        ldap: openldap
        dependsOn:
          - ldapLocalServer
      parameterFiles:
        - document: data2-minio-parameters
          unwrap: minio
          wrap: admin
