
# At this place as we want to exclude this in case of local deployment

loaders:

  - name: kad-components
    source:
      gitRepository:
        name: kad-components
    kadFiles:
      - components
      - templates
    rules:
      component: '^.*$'
      template: '^.*$'
      document: '^.*$'
