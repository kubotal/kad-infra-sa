

gitRepositories:


loaders:
  - name: kad-components
    source:
      gitRepository:
        name: kad-components
    kadFiles:
      - components
      - templates
    rules:
      component: '^.*$'
      template: '^.*$'
